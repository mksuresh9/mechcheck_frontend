# mechcheck-api (starter)

Minimal Express + TypeScript starter for MechCheck API.

Quick start

Install dependencies:

```bash
npm install
```

Run in development:

```bash
npm run dev
```

Build and run:

```bash
npm run build
npm start
```

Environment

Copy `.env.example` to `.env` and set values.
