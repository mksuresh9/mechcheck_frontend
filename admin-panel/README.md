# MechCheck Admin Panel

Vite + React admin dashboard scaffold using Material UI and Recharts.

Run locally:

```bash
cd admin-panel
npm install
npm run dev
```

This is a scaffold with mocked data. Connect to the API and add auth, pagination, and real data endpoints.
